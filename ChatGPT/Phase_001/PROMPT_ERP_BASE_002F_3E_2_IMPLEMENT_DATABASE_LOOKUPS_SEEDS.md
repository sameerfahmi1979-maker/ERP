# PROMPT_ERP_BASE_002F_3E_2_CREATE_DATABASE_LOOKUPS_SEEDS_SQL_FOR_REVIEW_ONLY

Act as a senior ERP QA lead, Supabase/PostgreSQL RLS auditor, SaaS security tester, PostgreSQL migration architect, enterprise ERP master-data architect, UAE business compliance analyst, and database implementation reviewer.

## Phase

ERP BASE 002F.3E.2 — Database + Lookup Categories + Seed Values

## Mode

This is a SQL FILE GENERATION ONLY prompt.

You must create the SQL migration file for review.

Do not apply the SQL to the database.

Do not run the migration.

Do not use MCP apply_migration.

Do not use Supabase SQL editor execution.

Do not modify the remote database.

Do not modify app source files.

Do not create UI screens.

Do not create server actions.

Do not update sidebar.

Do not start 002F.3E.3.

## Critical Instruction

Sameer wants to review the SQL file first.

Therefore, your task is:

```text
Create the migration SQL file only.
Create the review report.
Stop.
```

After Sameer/Dina review and approval, a separate prompt will be used to apply/send the SQL to the database.

## Source Plan

Use this corrected approved plan as the source of truth:

```text
ERP_BASE_002F_3E_PEOPLE_CONTACTS_CRM_FOUNDATION_TECHNICAL_IMPLEMENTATION_PLAN_REV1.md
```

The original non-REV1 plan is superseded where there is any conflict.

## Required Output Files

Create exactly these two files:

```text
supabase/migrations/YYYYMMDDHHMMSS_erp_base_002f3e2_people_contacts_crm_database_lookups_seeds.sql
ERP_BASE_002F_3E_2_DATABASE_LOOKUPS_SEEDS_SQL_REVIEW_REPORT.md
```

Do not create any other file.

The SQL file must be complete and ready for review.

The report must summarize what the SQL file contains and confirm that it was not applied.

---

# 1. Current Approved Context

The following phases are complete:

```text
002F.3B — Global Lookup / Dropdown Engine
002F.3C — Core UAE Shared Master Data
002F.3C.1 — Geography & Locations
002F.3C.2 — Finance Basics / Commercial Readiness
002F.3C.3 — Units & Measurements
002F.3C.4C — Final Readiness Review / Master Data Gate
```

The approved 002F.3E REV1 plan confirms:

```text
No generic persons table.
No employee tables in 002F.3E.
Employees are deferred to 002F.3F — HR Master Data.
Use separate main tables:
- customers
- vendors
- subcontractors
- consultants
- government_authorities
- recruitment_agencies
```

---

# 2. Scope of This SQL File

The SQL file must include only:

```text
29 database tables
22 lookup categories
approximately 130 lookup values
4 grouped permissions
role permission assignments
RLS policies
indexes
constraints
updated_at triggers
table/column comments
basic verification queries in comments or report
```

No UI.

No TypeScript.

No server actions.

No sidebar.

No select components.

No test data unless explicitly marked as optional and non-production.

## 2.1 Tables To Create

Create these 29 tables:

### Main entity tables

```text
customers
vendors
subcontractors
consultants
government_authorities
recruitment_agencies
```

### Contact tables

```text
customer_contacts
vendor_contacts
subcontractor_contacts
consultant_contacts
government_authority_contacts
recruitment_agency_contacts
```

### Address tables

```text
customer_addresses
vendor_addresses
subcontractor_addresses
consultant_addresses
government_authority_addresses
recruitment_agency_addresses
```

### Document tables

```text
customer_documents
vendor_documents
subcontractor_documents
consultant_documents
government_authority_documents
recruitment_agency_documents
```

### Bank detail tables

Create only these 5 bank detail tables:

```text
customer_bank_details
vendor_bank_details
subcontractor_bank_details
consultant_bank_details
recruitment_agency_bank_details
```

Do not create:

```text
government_authority_bank_details
```

Reason:

```text
Government authorities do not require bank detail table in this phase.
Government fees are normally paid through government portals or official payment channels.
```

---

# 3. Mandatory Database Standards

Every table must follow existing ERP database standards.

Use:

```sql
id bigint generated by default as identity primary key
```

Do not use:

```sql
serial
uuid primary key
auth.users(id) for audit fields
```

Audit/status fields must use:

```sql
created_at timestamptz not null default now()
created_by bigint references public.user_profiles(id)
updated_at timestamptz not null default now()
updated_by bigint references public.user_profiles(id)
deactivated_at timestamptz
deactivated_by bigint references public.user_profiles(id)
deactivation_reason text
is_active boolean not null default true
is_locked boolean not null default false
is_system boolean not null default false
sort_order integer not null default 0
```

Use the existing `public.set_updated_at()` trigger function if it exists.

Do not recreate foundation helper functions unless missing.

Inspect existing migrations first and follow current naming/style.

---

# 4. Lookup Categories and Seed Values

Use existing:

```text
global_lookup_categories
global_lookup_values
```

Do not create new lookup tables.

Do not hardcode dropdown values into app source.

Seed the categories and values into `global_lookup_categories` and `global_lookup_values`.

SQL must be idempotent.

Use `ON CONFLICT` or safe upsert logic consistent with existing lookup migrations.

## 4.1 Required Lookup Categories

Create/seed at least these 22 categories:

```text
PARTY_STATUS_TYPES
CUSTOMER_TYPES
CUSTOMER_SEGMENTS
VENDOR_TYPES
VENDOR_CATEGORIES
SUPPLIER_CATEGORIES
SUBCONTRACTOR_TYPES
SUBCONTRACTOR_CATEGORIES
CONSULTANT_TYPES
CONSULTANT_CATEGORIES
GOVERNMENT_AUTHORITY_TYPES
GOVERNMENT_AUTHORITY_CATEGORIES
JURISDICTION_LEVEL_TYPES
RECRUITMENT_AGENCY_TYPES
RECRUITMENT_AGENCY_CATEGORIES
INDUSTRY_TYPES
CRM_LEAD_SOURCES
CONTACT_TYPES
COMMUNICATION_PREFERENCE_TYPES
ADDRESS_TYPES
PARTY_DOCUMENT_TYPES
BANK_ACCOUNT_TYPES
```

## 4.2 PARTY_STATUS_TYPES

Seed:

```text
ACTIVE
INACTIVE
BLACKLISTED
ON_HOLD
UNDER_REVIEW
```

## 4.3 CUSTOMER_TYPES

Seed all:

```text
NORMAL_CUSTOMER
MAIN_CONTRACTOR
EPC_CONTRACTOR
GOVERNMENT_CUSTOMER
SEMI_GOVERNMENT_CUSTOMER
UTILITY_COMPANY
WATER_POWER_PLANT
INDUSTRIAL_CUSTOMER
COMMERCIAL_CUSTOMER
SCRAP_BUYER
SCRAP_SUPPLIER
PARTNER_CUSTOMER
```

Important:

```text
GOVERNMENT_CUSTOMER, SEMI_GOVERNMENT_CUSTOMER, UTILITY_COMPANY, and WATER_POWER_PLANT must be customer types, not only customer segments.
```

## 4.4 VENDOR_TYPES

Seed all:

```text
SUPPLIER
MATERIAL_SUPPLIER
EQUIPMENT_SUPPLIER
SERVICE_PROVIDER
INSURANCE_COMPANY
PROPERTY_LESSOR
VEHICLE_LESSOR
EQUIPMENT_LESSOR
CAMP_ACCOMMODATION_LESSOR
UTILITY_PROVIDER
TRANSPORTER
TRANSPORT_SERVICE_PROVIDER
LOGISTICS_SERVICE_PROVIDER
PRIVATE_WASTE_DISPOSAL_FACILITY
WASTE_DISPOSAL_SERVICE_PROVIDER
```

## 4.5 SUBCONTRACTOR_TYPES

Seed all:

```text
CIVIL_SUBCONTRACTOR
MANPOWER_SUBCONTRACTOR
TRANSPORTER
TRANSPORT_SUBCONTRACTOR
DEMOLITION_SUBCONTRACTOR
EQUIPMENT_SUBCONTRACTOR
SPECIALIZED_SUBCONTRACTOR
PARTNER_SUBCONTRACTOR
```

## 4.6 CONSULTANT_TYPES

Seed all:

```text
ENGINEERING_CONSULTANT
HSE_CONSULTANT
LEGAL_CONSULTANT
TECHNICAL_CONSULTANT
ENVIRONMENTAL_CONSULTANT
AUDIT_CONSULTANT
```

## 4.7 GOVERNMENT_AUTHORITY_TYPES

Seed all:

```text
LICENSE_ISSUER
PERMIT_ISSUER
REGULATOR
MUNICIPALITY
POLICE
CIVIL_DEFENSE
ENVIRONMENTAL_AUTHORITY
FREE_ZONE_AUTHORITY
PORT_AUTHORITY
CUSTOMS_AUTHORITY
PORT_CUSTOMS_AUTHORITY
UTILITY_AUTHORITY
TRANSPORT_AUTHORITY
WASTE_DISPOSAL_FACILITY
GOVERNMENT_WASTE_DISPOSAL_AUTHORITY
MINISTRY
```

## 4.8 RECRUITMENT_AGENCY_TYPES

Seed all:

```text
LOCAL_RECRUITMENT_AGENCY
OVERSEAS_RECRUITMENT_AGENCY
MANPOWER_SUPPLY_AGENCY
EXECUTIVE_SEARCH_AGENCY
```

## 4.9 Other Lookup Categories

Use the REV1 plan to seed full values for:

```text
CUSTOMER_SEGMENTS
VENDOR_CATEGORIES
SUPPLIER_CATEGORIES
SUBCONTRACTOR_CATEGORIES
CONSULTANT_CATEGORIES
GOVERNMENT_AUTHORITY_CATEGORIES
JURISDICTION_LEVEL_TYPES
RECRUITMENT_AGENCY_CATEGORIES
INDUSTRY_TYPES
CRM_LEAD_SOURCES
CONTACT_TYPES
COMMUNICATION_PREFERENCE_TYPES
ADDRESS_TYPES
PARTY_DOCUMENT_TYPES
BANK_ACCOUNT_TYPES
```

Keep values system-safe and editable where appropriate.

---

# 5. Business Classification Rules To Document in SQL Comments and Report

The SQL file should include comments documenting these classification rules.

## Customers

Use customers for:

```text
normal customer
government customer
semi-government customer
utility company
water & power plant
main contractor
EPC contractor
scrap buyer
scrap supplier
industrial customer
commercial customer
partner customer
```

## Vendors

Use vendors for:

```text
supplier
material supplier
equipment supplier
service provider
insurance company
lessor
property/yard lessor
vehicle lessor
equipment lessor
camp/accommodation lessor
transporter as service provider
logistics service provider
private waste disposal facility
private waste disposal service provider
utility provider
```

## Subcontractors

Use subcontractors for:

```text
civil subcontractor
manpower subcontractor
transport subcontractor
transporter when subcontracted for project execution
demolition subcontractor
equipment subcontractor
specialized subcontractor
partner subcontractor
```

## Government authorities

Use government authorities for:

```text
license issuer
permit issuer
regulator
municipality
civil defense
police
environmental authority
free zone authority
port authority
customs authority
port/customs authority
utility authority
transport authority
government waste disposal authority
government disposal facility
ministry
```

## Flexible classifications

Document:

```text
Transporter can be vendor or subcontractor depending on business case.
Waste disposal facility can be government authority or vendor depending on ownership/service model.
Recruitment agencies are separate but vendor-like for payment and bank detail purposes.
Partners are not separate table; they are customer or subcontractor classification.
```

---

# 6. Main Entity Table Requirements

## 6.1 customers

Required columns:

```text
customer_code
customer_name_en
customer_name_ar
customer_type_code
industry_type_code
customer_segment_code
lead_source_code
trn
trade_license_number
license_expiry_date
website_url
primary_email
primary_phone
primary_mobile
country_id
emirate_id
city_id
area_zone_id
address_line_1
address_line_2
po_box
makani_number
currency_id
payment_term_id
tax_type_id
credit_limit
credit_days
sales_owner_user_profile_id
notes
status_code
standard audit/status fields
```

FKs:

```text
country_id → countries(id)
emirate_id → emirates(id)
city_id → cities(id)
area_zone_id → areas_zones(id)
currency_id → currencies(id)
payment_term_id → payment_terms(id)
tax_type_id → tax_types(id)
sales_owner_user_profile_id → user_profiles(id)
```

## 6.2 vendors

Required columns:

```text
vendor_code
vendor_name_en
vendor_name_ar
vendor_type_code
vendor_category_code
supplier_category_code
trn
trade_license_number
license_expiry_date
website_url
primary_email
primary_phone
primary_mobile
country_id
emirate_id
city_id
area_zone_id
address_line_1
address_line_2
po_box
makani_number
currency_id
payment_term_id
tax_type_id
default_bank_id
notes
status_code
standard audit/status fields
```

`default_bank_id` should reference existing master table:

```text
banks(id)
```

Do not reference vendor bank details from the main vendor table in this phase to avoid circular dependency.

## 6.3 subcontractors

Required columns:

```text
subcontractor_code
subcontractor_name_en
subcontractor_name_ar
subcontractor_type_code
subcontractor_category_code
trn
trade_license_number
license_expiry_date
website_url
primary_email
primary_phone
primary_mobile
country_id
emirate_id
city_id
area_zone_id
address_line_1
address_line_2
po_box
makani_number
currency_id
payment_term_id
tax_type_id
hse_prequalification_status_code
worker_supply_allowed
equipment_supply_allowed
notes
status_code
standard audit/status fields
```

If `HSE_PREQUALIFICATION_STATUS_TYPES` is needed, add it as lookup category or use an existing HSE/prequalification lookup if it already exists.

## 6.4 consultants

Required columns:

```text
consultant_code
consultant_name_en
consultant_name_ar
consultant_type_code
consultant_category_code
trn
trade_license_number
license_expiry_date
website_url
primary_email
primary_phone
primary_mobile
country_id
emirate_id
city_id
area_zone_id
address_line_1
address_line_2
po_box
makani_number
currency_id
payment_term_id
tax_type_id
notes
status_code
standard audit/status fields
```

## 6.5 government_authorities

Required columns:

```text
authority_code
authority_name_en
authority_name_ar
authority_type_code
authority_category_code
jurisdiction_level_code
website_url
primary_email
primary_phone
primary_mobile
country_id
emirate_id
city_id
area_zone_id
address_line_1
address_line_2
po_box
notes
status_code
standard audit/status fields
```

Do not include TRN unless Cursor finds a strong requirement.

Do not include bank detail relation.

## 6.6 recruitment_agencies

Required columns:

```text
agency_code
agency_name_en
agency_name_ar
agency_type_code
agency_category_code
country_id
emirate_id
city_id
area_zone_id
address_line_1
address_line_2
po_box
primary_email
primary_phone
primary_mobile
website_url
license_number
license_expiry_date
countries_served
currency_id
payment_term_id
tax_type_id
notes
status_code
standard audit/status fields
```

For `countries_served`, choose safest approach:

```text
Option A: text[] country codes
Option B: recruitment_agency_countries child table
```

Use REV1 recommendation unless there is a better safer approach.

For phase 002F.3E.2, keep scope controlled. If junction table is too much, use text[] with clear comments and future enhancement note.

---

# 7. Child Table Requirements

## 7.1 Contact tables

Create:

```text
customer_contacts
vendor_contacts
subcontractor_contacts
consultant_contacts
government_authority_contacts
recruitment_agency_contacts
```

Each must include:

```text
parent_id FK
contact_code
contact_name_en
contact_name_ar
designation
department
contact_type_code
email
mobile
phone
whatsapp
is_primary
is_authorized_signatory
is_decision_maker
is_finance_contact
is_operations_contact
preferred_communication_code
notes
status_code
standard audit/status fields
```

Use parent-specific FK names if better:

```text
customer_id
vendor_id
subcontractor_id
consultant_id
government_authority_id
recruitment_agency_id
```

Prefer parent-specific FK names for clarity.

## 7.2 Address tables

Create:

```text
customer_addresses
vendor_addresses
subcontractor_addresses
consultant_addresses
government_authority_addresses
recruitment_agency_addresses
```

Each must include:

```text
parent FK
address_type_code
country_id
emirate_id
city_id
area_zone_id
address_line_1
address_line_2
building_name
street_name
po_box
makani_number
latitude
longitude
is_primary
is_billing_address
is_shipping_address
notes
status_code
standard audit/status fields
```

## 7.3 Document tables

Create:

```text
customer_documents
vendor_documents
subcontractor_documents
consultant_documents
government_authority_documents
recruitment_agency_documents
```

Each must include:

```text
parent FK
document_type_code
document_name
document_number
issue_date
expiry_date
has_expiry
expiry_reminder_days
file_path
is_required
is_verified
verified_by
verified_at
notes
status_code
standard audit/status fields
```

`verified_by` must reference:

```text
user_profiles(id)
```

## 7.4 Bank detail tables

Create:

```text
customer_bank_details
vendor_bank_details
subcontractor_bank_details
consultant_bank_details
recruitment_agency_bank_details
```

Each must include:

```text
parent FK
bank_id FK → banks(id)
bank_account_type_code
account_name
account_number
iban
swift_code
currency_id FK → currencies(id)
is_primary
is_active
notes
standard audit fields
```

Do not create government_authority_bank_details.

---

# 8. Constraints and Indexes

Add sensible constraints:

```text
code fields unique
TRN format check where TRN exists: 15 digits or null
email format check
website URL check if safe
credit_limit >= 0
credit_days >= 0
latitude between -90 and 90
longitude between -180 and 180
issue_date <= expiry_date where both exist
expiry_reminder_days >= 0
IBAN format check if safe
SWIFT format check if safe
```

Be careful with overly strict constraints that may reject valid UAE/international data.

If uncertain, prefer validation in app later rather than overly strict DB constraint.

Add indexes on:

```text
code fields
name fields
type/status code fields
country/emirate/city/area FKs
currency/payment/tax FKs
parent FKs in child tables
is_active
is_locked
is_system
expiry_date for documents
trn where applicable
```

---

# 9. Permissions

Use grouped permissions:

```text
master_data.party_master.view
master_data.party_master.manage
master_data.party_master.export
master_data.party_master.audit_view
```

Seed them into existing permissions table using current project pattern.

Assign roles:

```text
system_admin: all
group_admin: all
company_admin: view/export
branch_admin: view
normal users: none by default
```

If project role/permission table structure differs, follow existing migration patterns.

---

# 10. RLS Policies

Enable RLS on all 29 tables.

Use existing helper functions only.

Do not invent helper functions.

Likely helpers include:

```text
current_user_has_permission()
current_user_has_role()
current_user_profile_id()
```

For all tables:

## SELECT

```text
is_active = true
OR current_user_has_permission('master_data.party_master.view')
OR current_user_has_role('system_admin')
```

## INSERT

```text
current_user_has_permission('master_data.party_master.manage')
OR current_user_has_role('system_admin')
```

## UPDATE

```text
(current_user_has_permission('master_data.party_master.manage') OR current_user_has_role('system_admin'))
AND (is_locked = false OR current_user_has_role('system_admin'))
AND (is_system = false OR current_user_has_role('system_admin'))
```

## DELETE

```text
current_user_has_role('system_admin')
```

If child bank details should be more restricted, document recommendation but keep phase manageable unless required.

---

# 11. Numbering System Integration

Very important.

For each main entity, add document/numbering types to the existing Global Numbering Engine if required by current pattern.

Required entity codes:

```text
customer_code
vendor_code
subcontractor_code
consultant_code
authority_code
agency_code
```

Use simple prefixes:

```text
CUST
VEND
SUBC
CONS
AUTH
AGCY
```

No company/branch/year/month/day unless existing numbering system requires.

Follow existing 002F.2 global numbering engine pattern.

Do not create separate numbering logic.

---

# 12. Idempotency and Safety

The SQL file must be safe for review and likely safe to run once.

Use:

```sql
create table if not exists
create index if not exists
insert ... on conflict do update / do nothing
alter table ... add column if not exists
drop policy if exists before create policy if needed
```

Do not drop existing tables.

Do not drop existing columns.

Do not truncate data.

Do not delete existing lookup values.

Do not disable RLS.

Do not weaken existing security.

---

# 13. Required SQL File Organization

Structure the SQL file clearly:

```text
Header
Safety notes
Classification rule comments
Pre-check comments
Section 1: Lookup categories
Section 2: Lookup values
Section 3: Permissions and role assignments
Section 4: Main entity tables
Section 5: Contact tables
Section 6: Address tables
Section 7: Document tables
Section 8: Bank detail tables
Section 9: Indexes
Section 10: Triggers
Section 11: RLS policies
Section 12: Numbering rule/document type seeds
Section 13: Comments
Section 14: Verification queries as comments
```

---

# 14. Required Review Report

Create:

```text
ERP_BASE_002F_3E_2_DATABASE_LOOKUPS_SEEDS_SQL_REVIEW_REPORT.md
```

The report must include:

1. Phase name.
2. Date.
3. Statement that SQL file was created for review only.
4. Statement that SQL was NOT applied to database.
5. Files created.
6. Source REV1 plan used.
7. Tables included.
8. Tables excluded.
9. Lookup categories included.
10. Key lookup values included.
11. Permissions included.
12. RLS policy summary.
13. Numbering rules included.
14. Constraints and indexes summary.
15. Known risks or review points.
16. Items requiring Sameer/Dina review before database application.
17. Final status.

At the end write exactly:

```text
READY FOR REVIEW — SQL file created only and not applied to database.
```

## Final Instruction

Create only the SQL file and the review report.

Do not apply the SQL to the database.

Do not implement app code.

Do not start the next phase.
