feat(users,email,reports): USERS.4-5 permission flow, report schedule email fix

USERS.4 - Permission-Aware Navigation
- Fix getAuthContext() RLS bootstrap deadlock: use createAdminClient() for
  roles/permissions lookups; cookie client for auth only
- Add permission-aware sidebar filtering in AppSidebar
- Add /start smart post-login redirect using getFirstPermittedRoute()
- Add /access-denied and /no-access pages
- Move WorkspaceProvider into ErpShell for server-side defaultRoute injection
- Bump workspace localStorage version v4 to clear stale pinned tabs
- Add /dev/auth-debug diagnostic page
- Bulk replace redirect to /access-denied in permission guards
- Add /admin/hr/ smart redirect page
- Add route-access-registry.ts with 40+ protected routes

USERS.5 - Security Hardening Planning
- Add ERP_USERS_5_SECURITY_HARDENING_AND_AUDIT_ENHANCEMENT_PLAN.md

Email and Report Schedules fix
- sendExportEmail now uses getDefaultEmailProvider() from erp_email_provider_configs
  instead of env vars; resolves "Email service is not configured" error
- Add EmailMessageAttachment type and attachments support to EmailMessageInput
- Add attachment handling in MicrosoftGraphEmailProvider.sendEmail()
- Loosen SendEmailResult.provider from literal to string type
- Fix hardcoded microsoft_graph provider string in schedules.ts delivery log
- Fix moduleCode REPORTS to reports to fix case mismatch in permission check
