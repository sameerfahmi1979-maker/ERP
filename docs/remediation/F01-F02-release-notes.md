# F01 containment and F02 foundation checkpoint

This is the historical first-publication checkpoint. For subsequent lint, schema-baseline and configuration work, see [the F02 follow-up](F02-foundation-follow-up.md); the original evidence below is retained.

This change publishes the reviewed source for the completed containment release, plus a separately verified UI/engineering follow-up. It is **not closure of the entire ERP remediation programme or full F02**.

## Included

- Scoped role evaluation, credential/provider boundaries, restricted database function grants, sensitive RLS corrections, and authenticated internal scheduler gates.
- Strict application type/build configuration, generated/domain type separation, dependency compatibility repairs and maintained regressions.
- UI follow-up: deterministic theme-toggle hydration, accessible theme control, correctly scoped login animation and an explicit supported font fallback.
- UTF-8 schema types generated by a reproducible read-only command from the approved schema.
- Verified TLS in development/browser tests; no blanket certificate bypass.
- Secret-free, pinned GitHub verification workflow with a strict whole-application lint job and portable offline regression tests.
- Current-content credential redactions and a scanner that reports locations/categories, never matched values.

## Release distinction

The earlier three reviewed migrations and containment application/Edge deployment were applied and verified under the owner's controlled-release approval. This Git branch also includes later UI/tooling changes; those follow-up changes are **not automatically deployed merely by publishing the branch**. No workflow in this change deploys or migrates production.

No production business records, attachments or queued business events were changed by the UI follow-up. Credentials, raw logs, audit evidence, private recovery material and production data are deliberately excluded from this publication.

## Verification and remaining gates

Local shipping typecheck and strict production-style build pass. Existing application and remediation unit suites pass. The header regression fails on the pre-repair header and passes after repair. A synthetic untrusted TLS certificate is rejected; a valid HTTPS login-page request succeeds. Dark/light production-build reloads are checked in the isolated synthetic environment.

The broader lint inventory still contains legacy blocking diagnostics in workspace/forms, reporting, DMS, HR and shared components. They are **not suppressed**. The CI lint job is expected to block phase closure until these are addressed. This checkpoint should remain a draft review, not be merged by bypassing failing checks. Historical migration-lineage reconciliation, broader configuration contracts and module-specific browser acceptance remain separate required F02/later-phase work.

Do not apply the entire legacy migration directory blindly to a fresh or live database. The newly reviewed migration versions must retain their original bodies; later corrections require new migrations. The earlier live application must not be rolled back to revoked credentials or known exposed grants.

Historical provider-key revocation has been confirmed through the provider console. Existing Git history was not rewritten. Removing historical copies from reachable commits, clones and caches requires a separately coordinated operation, not a force push bundled into this publication.

See [engineering commands](../../tooling/README.md). Private detailed evidence remains in the owner's local audit-results folder.
