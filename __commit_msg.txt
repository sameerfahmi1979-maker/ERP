feat(ui): ERP GLOBAL UI.5 list standard rule + All Documents filter bar redesign

ERP GLOBAL UI.5 — Universal List UI Standard (rule only, no pilot migration yet):
- Add .cursor/rules/erp-global-list-ui-standard.mdc using All Documents as the
  approved visual reference for search bar, filter panel, table chrome, row
  actions, badges, sorting, column resize, pagination, and empty states.
- Add implementation_Review/ERP_GLOBAL_UI_5_UNIVERSAL_LIST_UI_STANDARD_RULE_REVIEW.md
  with inspected files, shared components, risks, and UI.5A next-phase suggestion.

All Documents list UX (aligns with the new rule reference):
- Two-row toolbar: search row + labeled filter panel in bordered bg-muted/10 grid.
- Replace filter Select dropdowns with searchable ERPCombobox (Type, Category,
  Status, Confidentiality, Expiry) with allowClear and h-8 text-xs triggers.
- Active filter chips with per-filter remove + Clear all.
- Filter state uses null (unset) instead of "all" sentinel.
